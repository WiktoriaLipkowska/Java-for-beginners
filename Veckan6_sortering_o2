/*
 2. Skapa ett program där användaren kan ange ett ord och programmet sedan 
visar alla bokstäverna i ordet sorterade. OBS ordet får enbart innehålla tecken (a-z).
Ange ett valfritt ord:
stockholm
c, h, k, l, m, o, o, s, t
 */
package veckan6_o2_sortering;

import java.util.Arrays;
import java.util.Scanner;

/**
 *
 * @author wiktorialipkowska
 */
public class Veckan6_o2_sortering {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
    Scanner input = new Scanner(System.in);
    
    System.out.println("Ange ett valfritt ord:");
    String word = input.nextLine(); 
    char[] arr = word.toCharArray();//string to array of chars

    for(int i = 0; i < arr.length; i++);
    Arrays.sort(arr);
    //Visa resultatet
    for (int number=0; number<arr.length;number++) {
        System.out.print(" ");
        System.out.print(arr[number] +", "); }
}}